{% extends "base.html.twig" %}

{% block title %}
	Ajouter un commentaire
{% endblock %}
{% block content %}

	<h3 class="mt-3">
		Bonjour
		<span class="text-danger">{{user.firstName}}
			{{user.lastName}}</span>
	</h3>


	{% if product.isProductFromUser(app.user) %}

		{% if product.getCommentFromUser(user) is null %}

			<p>
				Vous pouvez laisser un commentaire pour le produit
				<span class="text-danger">{{product.name}}
				</span>
			</p>

			{{form(form)}}

				{% else %}

					<p>
						vous avez déja  laisser un commentaire pour le produit
						{{product.name}}</p>
			{% endif %}

		{% else %}
			<p>
				vous n'avez pas acheter le produit
				{{product.name}}</p>
		{% endif %}

	{% endblock %}
